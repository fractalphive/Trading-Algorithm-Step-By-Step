# Commands — EP__

Run these in order. Each block is one step from the episode.

```bash
# 1. Install the algo's libraries (from repo root)
pip install -r requirements.txt

# 2. Add your Alpaca paper keys
cp .env.example .env
#   then paste your key/secret into .env

# 3. Run the code (from this folder)
cd episodes/epNN-slug
python code.py
```
